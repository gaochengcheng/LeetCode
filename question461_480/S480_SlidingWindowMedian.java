package question461_480;

import java.util.Arrays;

/**
 * 给定一个数组，和K值。
 * K表示滑动窗口的大小，滑动窗口从最左端往最右端移动，每次计算窗口中的中位数，依次把所有中位数放到一个数组中返回。
 * 
 * 知识点：滑动窗口使用
 * @author chengcheng
 * @time 2017年1月8日 下午12:11:47
 *
 */
public class S480_SlidingWindowMedian {
	public double[] medianSlidingWindow(int[] nums, int k) {
		return null;
	}
	public float median(int[] nums){
		Arrays.sort(nums);
		if(nums.length %2 ==0){
			
		}
		else{
			
		}
		return 0;
	}
}
